# Vault Project Handoff

**Updated:** 2026-08-29
**Current milestone:** H1 — private-transfer production foundation
**Maturity:** production-intent, unaudited, not activated, not safe for real funds

## Start here

This file exists so development can continue safely after a ChatGPT/Codex account,
task, or session change. Do not infer project status from a prompt such as
"continue". Before changing code:

1. Read `AGENTS.md`, `docs/PRODUCTION_STANDARD.md`, `docs/ROADMAP.md`, and the
   specifications for the component being changed.
2. Inspect the complete working tree and version-control state.
3. Run `./scripts/check.sh` and record any baseline failure before editing.
4. State the exact roadmap item and maturity classification being advanced.
5. Keep the change inside that item; do not create a prototype or silently add a
   trusted, mock, fail-open, or unbounded production path.

## Product objective

Vault is intended to become a privacy-first, permissionless blockchain with a
maximum supply of 21 million VLT, exact 0.5% transfer burn, metered execution,
private programmable contracts, permissionless exchange and BTC routes, and
durable application storage. These are roadmap objectives, not current claims.
Absolute anonymity, permanence, low fees, throughput, decentralization, or safety
must not be claimed until the corresponding model, implementation, measurements,
operational evidence, and independent reviews exist.

The user explicitly rejected disposable MVPs and demo-only security. All work must
target the deployable architecture while remaining honest about incomplete gates.

## Current implementation state

The repository contains substantial production-intent H1 work, including:

- canonical transfer-v2 actions, Ironwood V3 note encryption, note commitments,
  nullifiers, Merkle membership, randomized authorization, and fixed ciphertexts;
- a real Halo2 Action proof and composed accounting/burn constraints covering
  dummy actions, gas, conservation, exact ceiling 0.5% burn, change policy, burn
  ciphertext consistency, DKG descriptor binding, and the transfer effects digest;
- a fail-closed composite proof envelope that cannot be activated with only one
  proof component;
- canonical compact blocks, finalized-header authentication, local trial
  decryption, note-tree replay, encrypted transactional wallet storage, backups,
  birthday-frontier recovery, deterministic seed account discovery, and bounded
  restart-safe recovery coordination;
- output authorization, Noise XX pairing and KK signer transport, an encrypted
  peer registry, and crash-consistent Unix replay protection.

The latest completed cryptographic block is H1-C4. The normative record at
`docs/specs/BURN_AGGREGATION_V1.md` fixes canonical same-key aggregate
formation, a 128-effect and 16-public-window opening floor, indefinite
same-key carry-forward instead of timeout disclosure, aggregate-bound DLEQ
shares, deterministic filtering of malicious shares, and bounded
baby-step/giant-step recovery under the complete 21 million VLT supply cap.
The native type boundary does not accept an individual or low-volume ciphertext
for share generation. The full bound requires 144,913,768 baby steps and at
most the same giant steps; it is not run in normal transfer processing and
still requires H1-A1 target-hardware and cache benchmarks before activation.
DKG networking, finalized contribution admission, windows/epochs, share
publication/equivocation, and aggregate-supply state transitions remain H2.

H1-A1 remains open only for the consolidated external acceptance campaign.
H1-A2 has no further local feature work before its external/platform campaign,
and the local activation-hardening item is now H1-A3. The H1-A1 record at
`docs/research/H1-A1-PROOF-ENGINEERING.md` adds opt-in benchmark and artifact
reproduction tooling, an all-bucket three-sample local verification run, a
three-sample 2-Action proving run plus one 4/8/16 sample, a structured
malformed-envelope corpus, a fail-closed accounting-verifier deactivation
exercise, and a refreshed RustSec scan. On the local Apple M1/8 GiB host,
standalone verification medians ranged
from 129.659 ms to 236.361 ms; batch-of-four totals ranged from 173.250 ms to
376.437 ms. Reusable 2-Action proving plus immediate self-verification had a
22.115 s median; the single larger-bucket samples took 25.956 s, 25.362 s, and
49.232 s. Process maximum RSS ranged from 1,060,225,024 B to 1,199,996,928 B.
All four vectors regenerated byte-for-byte on this builder. These are
preliminary local results, not target-hardware acceptance.

H1-A1 remains open. `halo2_proofs 0.3.5` serializes parameters but not PLONK
PK/VK, so no ad hoc persistent key format was introduced. The selected bounded
startup design now reconstructs transparent parameters and PK/VK once in memory,
checks the canonical parameter and pinned-VK fingerprints, retains the material
for the process lifetime, and fails startup on mismatch. Local one-time costs
are accepted as preliminary evidence; target-hardware and all-bucket proving,
realistic heterogeneous batch workloads, sustained coverage-guided fuzzing,
two fresh isolated clean builds on the declared owned host, and the H1-C4
full-bound recovery/cache benchmark remain required. The dependency treatment
is now finite and automated:
`scripts/audit.sh` denies every vulnerability and warning except the exact
inactive `RUSTSEC-2023-0089`, then separately fails if `atomic-polyfill` enters
the active graph of the root, Halo2 release, or fuzz workspace. The benchmark,
fuzz, and reproduction scripts are explicit opt-in tools and are not called by
normal gates or transfer processing. No further local implementation or design
item remains inside H1-A1. The consolidated campaign's deterministic
mixed-bucket validator runner is complete and must be run on the owned
acceptance host with the other A1 harnesses rather than as a separate campaign.

The fuzz-only `zk/halo2/fuzz` workspace and opt-in
`scripts/fuzz-zk-halo2.sh` now pin `cargo-fuzz 0.13.2`,
`libfuzzer-sys 0.4.13`, and `nightly-2026-08-20` without adding them to the
release workspace. Its composite-envelope target combines raw input with
structured deep-path mutation and checks all Action counts plus suite-bound
round trips. A 300-second Apple M1 AddressSanitizer smoke run and a 10-second
corpus-resume check completed without crash, timeout, sanitizer finding, or
crash artifact; the latter executed 158,901 inputs at 14,445/s with 367 MiB
peak RSS. This validates the harness only and does not satisfy the remaining
sustained-fuzzing gate.

The current H1-C4 in-memory BSGS table was also measured through 4,194,304
baby/giant steps with the opt-in `scripts/benchmark-burn-recovery.sh` harness.
That run used 345,702,400 B RSS, built in 70.656 s, and recovered the worst
endpoint in 69.241 s. Linear full-bound projection is approximately 11.94 GB
RSS and 40 minutes for each phase on the local M1/8 GiB host. The guarded
full-bound run was not attempted because it would exceed physical memory. No
lower bound, alternative algorithm, or policy ID was adopted. H1-A1 now
defines a canonical versioned cache containing only the increasing baby-step
point sequence, with exact scheme/policy/bound metadata and caller-trusted
BLAKE3 integrity. It reconstructs the same randomized in-memory `HashMap`
rather than persisting its layout. At 4,194,304 steps the 134,217,868-byte cache
built and synchronized in 72.663 s, validated and restarted in 1.405 s, and
recovered the worst endpoint in 67.346 s with 349,585,408 B RSS. The exact
full-bound file length is 4,637,240,716 bytes; its two fresh cache builds,
matching digests, restart, memory, and recovery still require the owned
acceptance host.

The opt-in `scripts/reproduce-zk-halo2-build.sh` now builds two physically
separate clean target trees offline and locked behind the same normalized
logical target path. Earlier direct target paths produced different Rust metadata
while native objects and the final executable already matched, so path
normalization is an enforced part of the reproduction contract. The current
2026-08-26 clean repetition, after adding the policy-bound burn-cache API,
matched both targets byte-for-byte: core `rlib` `7313840d...050324` and setup
manifest `5ce85faf...87ee4`. This same-host evidence shape is now the accepted
H1-A1 requirement, with the common-mode limitation recorded in the closure
matrix.

The opt-in `scripts/benchmark-zk-halo2-heterogeneous.sh` runner constructs all
four pinned verifier materials and times exact deterministic `common`,
`balanced`, and `max-heavy` block profiles. The local common-profile smoke run
verified `[2,2,2,2,4,4,8,16]` in 1,239.336 ms after a 133,911.171 ms one-time
all-suite startup, with 209,534,976 B maximum RSS. These are runner-validation
numbers, not target-validator acceptance.

H1-A2 is tracked in `docs/research/H1-A2-WALLET-HARDENING.md`. Its first local
closure implements the actual historical schema-1 to schema-2 wallet migration
rather than inventing a schema 3. Migration authenticates legacy state, requires
a separate no-clobber backup, permits only genesis-origin schema-1 wallets,
reseals every private row under schema-2 AAD in one exclusive transaction, and
adds the canonical recovery record. Current restore upgrades an authenticated
legacy snapshot before publishing a new schema-2 destination. Tests preserve a
non-empty note and witness and inject rollback after each of nine migration
stages. Backup exports now also return exact copy receipts, bounded copy
verification rejects damage, and a restore-drill API exercises the complete
production restore path in protected disposable storage. The local rotation
profile requires three verified copies across two failure domains, including
one offline/immutable copy, before an older generation can be retired.
Checkpoint retention is now frozen at an authenticated immutable 1..4096
ordinary-checkpoint policy, and validated `VACUUM` compaction is implemented.
The bounded history runner covers unrelated/owned outputs, pruning, backup copy,
restore drill, compaction, page/row growth, and reopen. Local smoke runs passed
32-block unrelated and 8-block/16-note owned profiles. The opt-in large legacy
migration runner passed a two-block smoke test. A journal-observed `SIGKILL`
runner passed three recover/reopen cycles; guarded Linux ENOSPC and paired
`perf` leakage runners compile and are deferred to the external host. The next
local recovery UX mapping now forbids final balances/spending until `Ready` and
distinguishes rescan-required from unsupported account ranges. The exact
rollback protocol now binds full tip and database identity in a canonical
stable/pending record and wraps commits in secure CAS prepare/finalize; injected
post-commit failure and valid-old-database rollback tests pass. Root-key and
secure-store traits are ready, but real adapters remain physical platform work.
The approved English BIP-39 boundary is now implemented: 24-word/256-bit new
generation, checksum-valid standard imports, explicit NFKD passphrase, redacted
zeroizing mnemonic/passphrase/seed types, and the official vector. The pinned
dependency addition passes the offline RustSec gate. H1-A2 has no further local
feature work before its external/platform campaign; hard-reset/power-loss,
device faulting, real keychains/secure elements, secret-entry UX, large runs,
and independent review remain open. Continue locally with H1-A3 without
starting H2 or the long owned-host campaign yet.

H1-A3 is now tracked in
`docs/research/H1-A3-SIGNER-HARDENING.md`. Its first local item closes the gap
where durable peer revocation blocked only future handshakes: every
registry-issued handshake and transport now shares a peer lifecycle gate.
Revocation and rotation shut the old gate before the durable transition, and an
uncertain registry persistence failure shuts every active session as the handle
becomes poisoned. Adversarial tests cover established revoked/rotated sessions
and injected I/O failure. This is production-intent local enforcement, not
remote key erasure or independent review. The next local A3 closure also removes
raw public confirmation and lifecycle bypasses: XX pairing,
policy-validated/independently sourced transfer intent, revocation, and rotation
now require explicit trusted adapter traits, and the crate ships no permissive
implementation. Rejection does not consume replay state or mutate the peer
registry. Concrete trusted displays/input sources remain platform evidence. The
next local closure freezes the protected signer identity and rollback-resistant
replay contracts: canonical `VSKM`/`VSRS` records, no-clobber enrollment,
missing-state failure, complete-state secure CAS, exact pending-challenge
binding, and poison-on-uncertainty enforcement now pass six adversarial tests.
The existing Unix replay file remains explicitly crash-consistent but not
rollback-resistant. Real keychain/secure-element adapters remain physical
platform evidence. The next local closure freezes the re-randomized RedPallas
FROST product boundary without enabling the audit-blocked implementation:
canonical 2..16 participant policies, exact-threshold per-action commitment
sets, complete transcript/effects/action/`rk`/`alpha` agreements, mandatory
independent participant confirmation, durable one-use nonce and whole-attempt
abort rules, and verification of the final ordinary RedPallas signature in the
already approved signer session. Four contract tests and one positive/negative
session-path test pass, but they deliberately do not simulate FROST shares.
Concrete FROST key ceremony, protected share/nonce adapters, aggregation, and
device/review evidence remain open. The fifth local closure freezes delegated
proving without activating a remote service: local proving remains the default,
and each delegated job binds one exact policy, dedicated prover identity,
channel, durable counter, effects digest and complete witness commitment. The
trusted confirmation explicitly states that the prover learns the full transfer
witness plus the account's Orchard full-viewing key, including derivable IVKs,
OVKs, addresses and nullifiers for both scopes. It receives no seed, spending
key or spend signature, and every returned proof must verify
locally against the exact effects. Permanent revocation blocks future jobs but
cannot erase earlier endpoint disclosure. Seven delegated-proving contract
tests pass. A3-6 is now locally complete: bounded VDPW reconstruction and
VDPR/VDPS codecs cover all 2/4/8/16 buckets; 176 deterministic VLT2/VAOP/signer/
multisig/delegated/Noise artifacts reproduce byte-for-byte; the signer corpus
test is committed; and the pinned sanitizer fuzz plus latency/memory runners
are ready. The local 15-second fuzz smoke executed 11,164 cases with 223 MB
peak RSS and no finding. VDPS corpus proofs are honestly classified as
codec-positive/local-verifier-negative because they reuse H1-C3 proof bytes
under different effects; positive proof evidence remains the existing H1-C3
corpus. See `docs/specs/SIGNER_CORPUS_V1.md` and
`docs/research/H1-A3-SIGNER-HARDENING.md`.

The finite local A3 sequence is exhausted. H1-A3 remains incomplete because
reviewed FROST, protected platform/device adapters, physical trusted UX,
dedicated delegated-prover transport/store/suite adapters, endpoint privacy
evidence, sustained target-machine fuzzing and independent review remain open.
Do not start A4, H2, mainnet integration, the owned-host acceptance run, a
local threshold cryptography implementation or an actual remote prover
deployment without a new explicit decision.

All target-hardware compute is being accumulated in
`docs/research/H1-EXTERNAL-ACCEPTANCE-CAMPAIGN.md`. The 2026-08-28 owner
decision replaces the two-host rental with one owned 64 GiB/500 GB x86_64 Linux
acceptance host; no GPU is required. Two fresh isolated roots on that host must
match, but this is not independent reproduction. The accepted common-mode
CPU/firmware/OS/toolchain/filesystem/host-compromise risk must remain in A4
materials. A3-6 corpora and harnesses are ready, but do not start the long or
destructive run until the owned-machine hard-reset/device-fault plan is frozen.
OS keychains, secure
elements, and hardware-wallet evidence require actual supported platform
devices and cannot be substituted by generic VMs.

The read-only `scripts/preflight-h1-acceptance-host.sh` now freezes the owned
host admission checks without starting any acceptance workload. The 2026-08-29
run proves that the active Codex Mac is still the Apple M1/8 GiB development
machine, not the required Linux x86_64 acceptance host: it failed CPU, memory,
disk, OS/architecture, virtualization and Linux-tool checks as expected. Run
the preflight with an operator-defined machine label and absolute evidence root
on the owned powerful computer before preparing long or destructive
commands.

H1-C3 remains complete. The record at
`docs/research/HALO2-TRANSFER-V2-VECTORS.md` fixes one self-contained real-proof
conformance vector for each 2/4/8/16-Action Halo2 shape. Every vector binds the
synthetic private witness, canonical effects, exact public instances, distinct
suite ID, proof bytes and digest, plus an anchor mutation and a proof-byte
mutation with explicit accept/reject expectations. The 2/4/8 proofs are 9,600
bytes; the 16-Action proof is 9,664 bytes. Release verification accepts every
positive proof and rejects both mutations for every bucket. These suite IDs are
vector-locked evidence only: no consensus verifier admits them.

H1-C2 remains complete. The normative setup record at
`docs/architecture/HALO2_SETUP_AND_LIFECYCLE.md` fixes transparent parameter
derivation, exact parameter and VK fingerprints, the fixed Action VK,
reproduction requirements, fail-closed lifecycle rules, the non-selected RISC
Zero role, and the independent DKG trust assumption. The selected mapping is
`k = 14` for 2/4/8 and `k = 15` for 16 because the largest circuit does not fit
at `k = 14`.

H1-C1 remains complete: the isolated RISC Zero workspace implements the
transfer-v2 conformance statement in
`docs/research/RISC0-TRANSFER-V2-REFERENCE.md`. Its native positive and
field-mutation tests pass and the guest image is pinned. A real proving attempt
was intentionally terminated after approximately 2 hours 48 minutes without a
receipt. RISC Zero is not the selected transfer backend, and its receipt is not
an H1 closure gate. The oracle exposes no consensus verifier or state adapter.
The wallet recovery coordinator remains bounded by an external
consensus-verified header-source interface; the production full-node/light-client
adapter is H2 and does not yet exist.

The 2026-08-28 final root gate passed 153 workspace tests with one opt-in external
campaign ignored, formatting, Clippy with warnings denied, and rustdoc. The
isolated Halo2 gate passed 21 release-mode unit tests plus ten integration
tests, including all five delegated-witness cases. The earlier
2026-08-26 H1-A1 root gate passed 125 workspace tests, formatting, Clippy
with warnings denied, and rustdoc. The focused burn suite passed ten
native tests, including the canonical-cache integrity case, fixed
aggregate/recovery case, and adversarial
formation, privacy-floor, membership, bound, and malicious-share cases. The
non-default `reference-oracle` benchmark feature separately passed those ten
tests, all-target Clippy with warnings denied, and rustdoc. Locked
all-target Halo2 compilation and Clippy also passed after H1-C4; the circuit
shape did not change. The 2026-08-26 H1-A1 isolated Halo2 gate passed 21
release-mode unit tests plus five integration tests, including exact
parameter/VK reconstruction rejection cases, real Action, accounting/burn, and
all-bucket H1-C3 conformance verification. The setup
manifest locally regenerated all three parameter sets, four VK
fingerprints, four suite IDs, and exact proof lengths. The isolated RISC Zero
native suite currently
passes eight host tests plus its core arithmetic test, formatting, Clippy with
warnings denied, and rustdoc. No full transfer-v2 RISC Zero receipt was produced;
the historical accounting-only receipt remains the only measured zkVM proof.
Release-sensitive crate testing previously reported 74 tests.
The 2026-08-28 offline dependency gate loaded 1,226 advisories and scanned the
177-dependency root, 153-dependency Halo2 release, and 157-dependency fuzz
lockfiles. It found no known vulnerability or non-allowlisted warning and
confirmed `atomic-polyfill 1.0.3` is inactive in all three graphs. The warning
remains visible and narrowly allowlisted; re-run the gate rather than assuming
these numbers remain current.

## H1 scope correction

Earlier development mixed three different meanings of "finish H1": cryptographic
implementation, activation hardening, and mainnet eligibility. This caused broad
roadmap items to be decomposed only after prior work was reported complete. Do not
continue that pattern.

- **H1 cryptographic implementation** closes when reproducible proofs and vectors
  verify all H0 private-transfer invariants under the selected construction.
- **H1 activation hardening** includes wallet/key custody, private retrieval,
  platform adapters, operational fault testing, benchmarks, and independent
  review. These gates can block activation without making the cryptographic scope
  infinite.
- **H2 integration** owns real consensus, finality, nodes, snapshots, and light
  clients. A concrete recovery source backed by those mechanisms cannot be closed
  independently inside H1; H1 defines and tests its fail-closed boundary.
- **H3 contracts** remain blocked until the private-transfer, wallet-scanning, and
  network-origin entry gates stated in the roadmap are satisfied. Contract work
  must never weaken base-layer privacy.

The unchecked H1 roadmap entries are now reconciled in the frozen finite matrix
at [`H1_CLOSURE_MATRIX.md`](H1_CLOSURE_MATRIX.md). Continue them in ID order
within their classification. H1-C1 through H1-C4 are complete, so the finite H1
cryptographic implementation column is closed. This does not finish the H1
milestone: H1-A1 through H1-A4 remain mandatory activation hardening. Do not
invent another H1-C item. Real consensus/network adapters remain H2, while
review and benchmark gates remain activation hardening rather than new
cryptographic scope.

## Non-negotiable handoff rules

- Do not describe Vault, H1, or any component as production-ready merely because
  its implementation or tests exist. Use the maturity labels in the production
  standard.
- Do not use real funds.
- Do not start contracts merely because a wallet subtask finished.
- Do not claim "100% anonymous". State separately what is cryptographically hidden
  and what network, endpoint, timing, recovery, or side-channel metadata remains.
- Do not replace real proof verification, consensus, custody, BTC interoperability,
  or durable storage with a mock or centralized shortcut.
- Preserve existing worktree changes. No commit existed when this handoff was
  written, so all repository files were untracked and especially vulnerable to
  accidental loss.

## Suggested first prompt in a new account

> Open `/Users/tomascorzo/vault`. Read `AGENTS.md` and every document it requires.
> Inspect the entire working tree and run the baseline gates. Summarize the current
> H1 implementation, its honest maturity, and the finite closure matrix before
> editing. H1-C1 through H1-C4 are complete; do not invent another cryptographic
> item. Continue only an explicitly selected H1 activation-hardening item, not
> H2/mainnet work. Do not create prototypes, start contracts, use real funds, or
> claim production readiness.
